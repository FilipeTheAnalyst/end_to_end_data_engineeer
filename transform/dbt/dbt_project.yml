name: 'e2e_data_engineer'
version: '1.0.0'
config-version: 2

profile: 'e2e_data_engineer'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  e2e_data_engineer:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
